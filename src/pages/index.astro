---
import Layout from '../layouts/Layout.astro';
import Hub from '../components/Hub';
import { config } from '../config';

const projects = config.projects.map(p => ({
  title: p.title,
  desc: p.description,
  tags: p.tags,
  live: p.live,
  source: p.source,
  android: 'android' in p ? p.android : undefined,
}));
---

<Layout>
  <Hub
    client:idle
    avatar={config.avatar}
    name={config.name}
    tagline={config.tagline}
    bio={config.bio}
    resume={config.resume}
    github={config.github}
    linkedin={config.linkedin}
    email={config.email}
    location={config.location}
    education={config.education}
    hobbies={config.hobbies}
    aboutMd={config.about}
    skillsMd={config.skills}
    projects={projects}
    githubUsers={config.githubUsers}
    contact={config.contact}
    youtubeVideos={config.youtubeVideos}
    musicUrl={config.musicUrl}
    leetcode={config.leetcode}
    hackerrank={config.hackerrank}
  />
  <footer class="fixed bottom-20 md:bottom-4 left-1/2 -translate-x-1/2 z-40 text-[10px] text-stone-700 pointer-events-none select-none">
    Last updated June 2026
  </footer>
</Layout>
